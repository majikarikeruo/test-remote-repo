# Firestore Project ID (required for Cloud Run)
FIREBASE_PROJECT_ID=your-project-id

# Authentication password (optional, leave empty for no auth)
AUTH_PASSWORD=your-secret-password

# Port (optional, default: 3000)
PORT=3000

# For local development with Firebase credentials
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
